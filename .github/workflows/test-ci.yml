name: Test CI
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3

      - name: Setup tmate session
        if: always()
        uses: mxschmitt/action-tmate@v3
